# 0924：服务器准备文件，再完整重跑，仅运行 RobDP 8×

本地只维护代码和本手册。你提交并推送代码后，在服务器上复制必要输入、`git pull`，然后按顺序执行全部命令。**所有命令都在服务器的 Bash 中执行，没有 Mac 上传步骤，没有临时 runner。**

本次比较四个方法：Original PostgreSQL、Reqo-Gen + Reqo-Selector、RobDP-8 Direct、RobDP-Gen8 + Reqo-Selector。只有 `reqo_guc`、`robdp_last_level_8x1__0x0` 两个候选来源，两折分别训练，共四个模型。

SQL、global/fold error profiles 和数据库统计作为已有输入复用。baseline、候选生成、hint SQL、候选执行、编码、训练和汇总全部重新运行。成功执行使用 `Execution Time`；超时仍采用明确标记的 30,000 ms 惩罚值。

提交时需要包含两个 runner、Execution Time 相关修改、`Utils/plan_runtime.py` 和本手册。不要漏掉新模块，否则服务器 pull 后仍无法运行。本次不自动提交代码。

# 第一部分：准备全部文件（不运行实验）

## 0. 服务器上的统一设置

登录 `hx68@db-bench`，使用 Bash，启用原来运行 Reqo 的 Python 环境，然后执行本节。断线后重新执行本节即可恢复变量；本节不启动实验。

```bash
set -euo pipefail
export OLD_ROOT=/data/robdp
export BASE=/data/robdp-new
export REQO_ROOT=${BASE}/Reqo-PG
export RUN_ROOT=${BASE}/imdb-separate-ep-0924
export SQLS_DIR=${BASE}/imdb-error-profile-0924
export STATS_DIR=${REQO_ROOT}/Data/database_statistics
export DATASET_ROOT=${REQO_ROOT}/Data/imdbloadbase/datasets-separate-ep-0924
export TRAIN_RESULTS_ROOT=${REQO_ROOT}/Results/imdbloadbase/separate-ep-0924
export SUMMARY_DIR=${RUN_ROOT}/summary
export LOG_DIR=${RUN_ROOT}/logs
export DBNAME=imdbloadbase
export DBUSER=hx68
export HOST=localhost
export PORT=5432
export PGDATA_DIR=/winhomes/hx68/imdbloadbase
export WORKLOAD=cardinality
export QUERY_ID_LIMIT=100
export FOLD_COUNT=2
export SPLIT_SEED=0
export STMT_TIMEOUT=30s
export STMT_TIMEOUT_MS=30000
export REQO_PYTHON=${REQO_PYTHON:-python3}
PARAM_GROUPS=(8x1__0x0)
SOURCES=(reqo_guc robdp_last_level_8x1__0x0)
mkdir -p "${BASE}" "${RUN_ROOT}/inputs" "${LOG_DIR}"
```

所有新实验路径都位于 `BASE` 下，名称使用 `0924`。PostgreSQL 安装和已有 IMDb 集群不需要复制，继续使用 `/opt/pgsql/...` 和上述 PGDATA；完整重跑不包含重新导入数据库。

## 1. 准备：从服务器旧目录复制必要输入

只复制下表中的输入。**不复制旧 runner_outputs、hint SQL、plans_cache、encode.pt、datasets、checkpoint 或 summary。** 原件不变，目标目录名从 `0826` 改为 `0924`。

| 从旧路径读取 | 放到新路径 | 需要的文件 |
| --- | --- | --- |
| `/data/robdp/imdb-error-profile-0826/` | `${BASE}/imdb-error-profile-0924/` | 每模板的 `raw_data/*-0_testing.json` 和 `error_profile/*.txt` |
| `/data/robdp/old/imdb-separate-ep-0826-1/` | `${BASE}/imdb-separate-ep-0924-1/` | `error_profile/*.txt`、`owned_query_ids.csv`、`manifest.csv` |
| `/data/robdp/old/imdb-separate-ep-0826-2/` | `${BASE}/imdb-separate-ep-0924-2/` | 同上，属于 train fold 2 |
| `/data/robdp/imdb-separate-ep-0826/folds/` | `${RUN_ROOT}/inputs/folds-0924-reference/` | 四个 ownership CSV，用来核对 profile 和新 folds |
| `/data/robdp/Reqo-PG/Data/imdbloadbase/database_statistics/` | `${STATS_DIR}/` | 七个 `.npy`，只有新目录缺少时才补充 |

**不能只复制 error profile：原始 query SQL 也必须有。** runner 读取 `raw_data/<template>-0_testing.json`。已有的 `database_statistics.tar` 已解压到新目录，正常情况下七个 `.npy` 不需要再次复制。当前编码使用字符串哈希，不需要额外复制 Word2vec 模型。

你之前放在新目录里的 `*-0826` 文件夹或归档不用删除，也不会被新实验读取；本节直接从旧目录准备明确命名为 `*-0924` 的输入。目标文件已存在且内容一致就跳过，内容不同则停止。

### 1A. 复制 SQL、profiles 和 fold 参考文件

```bash
"${REQO_PYTHON}" - <<'PY'
import os, shutil
from pathlib import Path
old = Path(os.environ['OLD_ROOT'])
base = Path(os.environ['BASE'])
run = Path(os.environ['RUN_ROOT'])

def copy_input(src, dst):
    if not src.is_file():
        raise FileNotFoundError(src)
    if dst.exists():
        if dst.read_bytes() != src.read_bytes():
            raise RuntimeError(f'Existing input differs: {dst}')
        return
    dst.parent.mkdir(parents=True, exist_ok=True)
    shutil.copy2(src, dst)

specs = [
    (old / 'imdb-error-profile-0826', base / 'imdb-error-profile-0924', False),
    (old / 'old/imdb-separate-ep-0826-1', base / 'imdb-separate-ep-0924-1', True),
    (old / 'old/imdb-separate-ep-0826-2', base / 'imdb-separate-ep-0924-2', True),
]
for src_root, dst_root, fold_profile in specs:
    if not src_root.is_dir():
        raise FileNotFoundError(src_root)
    files = list(src_root.glob('*-0_cardinality/error_profile/*.txt'))
    if fold_profile:
        files += list(src_root.glob('*-0_cardinality/raw_data/owned_query_ids.csv'))
        files += [src_root / 'manifest.csv']
    else:
        files += list(src_root.glob('*-0_cardinality/raw_data/*-0_testing.json'))
    assert files, f'No inputs found in {src_root}'
    for src in files:
        copy_input(src, dst_root / src.relative_to(src_root))
    print('Prepared:', dst_root, 'files:', len(files))

for source in ('original', 'robdp'):
    for fold in (1, 2):
        name = f'{source}_fold_{fold}.csv'
        copy_input(old / 'imdb-separate-ep-0826/folds' / name,
                   run / 'inputs/folds-0924-reference' / name)
PY
```

### 1B. 服务器 pull 已提交的新代码

现有 `/data/robdp-new/Reqo-PG` 已经是 Git 仓库，直接 pull。只有目标代码仓库不存在时，才使用旧仓库配置的远端 URL clone；不是复制旧仓库中的 Data/Results。

确保服务器当前分支就是你提交并推送修改的分支。若 pull 报告本地修改冲突，先处理具体冲突，不用 `reset --hard` 覆盖服务器文件。

```bash
if [ ! -d "${REQO_ROOT}/.git" ]; then
  REQO_REMOTE=$(git -C "${OLD_ROOT}/Reqo-PG" remote get-url origin)
  git clone "${REQO_REMOTE}" "${REQO_ROOT}"
fi
cd "${REQO_ROOT}"
git branch --show-current
git status --short
git pull --ff-only
```

### 1C. 补齐统计文件、检查代码和环境

```bash
"${REQO_PYTHON}" - <<'PY'
import os, shutil
from pathlib import Path
old = Path(os.environ['OLD_ROOT'])
root = Path(os.environ['REQO_ROOT'])
stats = Path(os.environ['STATS_DIR'])
required = ['tables_index.npy', 'tables_index_all.npy', 'columns_index.npy',
            'columns_list.npy', 'attribute_range.npy',
            'postgresql_nodestypes_all.npy', 'table_columns_number.npy']
stats.mkdir(parents=True, exist_ok=True)
for name in required:
    dst = stats / name
    src = old / 'Reqo-PG/Data/imdbloadbase/database_statistics' / name
    if not dst.is_file():
        shutil.copy2(src, dst)
    elif dst.read_bytes() != src.read_bytes():
        raise RuntimeError(f'Database statistics differ from the source experiment: {dst}')
assert (root / 'Utils/plan_runtime.py').is_file(), 'Commit/push/pull Utils/plan_runtime.py too'
assert 'cache_timing_fields' in (root / 'Utils/reqo_encode_sql.py').read_text()
assert 'require_execution_time_dataset' in (root / 'train_no_split.py').read_text()
assert '--parameter-group' in (root / 'run_imdb_with_robdp.py').read_text()
print('Inputs, statistics and new code: OK')
PY

"${REQO_PYTHON}" - <<'PY'
import numpy, pandas, psycopg2, torch, torch_geometric, gensim, scipy, matplotlib, tqdm
print('torch:', torch.__version__, 'CUDA available:', torch.cuda.is_available())
PY

for version in 16.2.0000 16.2.0524 16.2.0626 16.2.hint; do
  test -x "/opt/pgsql/${version}/bin/pg_ctl"
done
test "$(cat "${PGDATA_DIR}/PG_VERSION")" = 16
```

## 2. 参数现在每次只运行一组

两个正式 runner 均使用必填参数 `--parameter-group`，不再自动枚举组合。本次后续命令都指定：

```text
--parameter-group 8x1__0x0
```

其含义是 `add_path_limit=8`、`main_objective_id=1`、`retain_path_limit=0`、`retain_strategy_id=0`，`final_score_id` 等于 local objective。磁盘结果目录仍为 `8x1/0x0`。

以后想单独跑 1×，把这一项改成 `1x1__0x0`；需要多个配置就分别调用 runner。旧的 `--main-objective-id-vals`、`--retain-strategy-id-vals`、`--path-limit` 参数已移除。旧手册不再直接适配这两个 runner；0924 全部使用新命令。


## 3. 重建 shared folds，并验证已有 profile 的归属

这是原步骤 3，必须在 0C 之前。仍用 32 个模板、每模板 100 个 query、两折、query granularity、seed 0。重建后同时核对旧 folds 和
profile 的 `owned_query_ids.csv`，任何不一致都停止；不能在重新随机划分后继续复用旧 fold profiles。

```bash
"${REQO_PYTHON}" "${REQO_ROOT}/build_imdb_fold_splits.py" \
  --sqls-dir "${SQLS_DIR}" --workload-name "${WORKLOAD}" \
  --skip-template-id-vals 29 --query-id-limit "${QUERY_ID_LIMIT}" \
  --output-root "${RUN_ROOT}" --fold "${FOLD_COUNT}" \
  --split-seed "${SPLIT_SEED}" --split-granularity query --warn-if-not-query-granularity \
  > "${LOG_DIR}/3-folds.log" 2>&1

"${REQO_PYTHON}" - <<'PY'
import os, csv
from pathlib import Path
from imdb_workload_common import load_sql_groups
base = Path(os.environ['BASE'])
run = Path(os.environ['RUN_ROOT'])
sqlroot = Path(os.environ['SQLS_DIR'])
sqls = load_sql_groups(sqlroot, 'cardinality', [29], 100)
assert len(sqls) == 32 and all(len(q) == 100 for q in sqls.values())
def keys(path):
    with path.open() as f:
        rows = list(csv.DictReader(f))
    result = {(int(r['template_id']), int(r['original_query_id'])) for r in rows}
    assert len(result) == len(rows), f'Duplicate query IDs: {path}'
    return result
owned = []
for fold in (1, 2):
    actual = keys(run / f'folds/robdp_fold_{fold}.csv')
    assert len(actual) == 1600
    assert actual == keys(run / f'inputs/folds-0924-reference/robdp_fold_{fold}.csv')
    assert actual == keys(run / f'folds/original_fold_{fold}.csv')
    profile_root = base / f'imdb-separate-ep-0924-{fold}'
    profile_keys = set()
    for template in sqls:
        d = profile_root / f'{template}-0_cardinality'
        assert list((d / 'error_profile').glob('*.txt')), f'Missing profile: {d}'
        profile_keys |= keys(d / 'raw_data/owned_query_ids.csv')
        assert list((sqlroot / f'{template}-0_cardinality/error_profile').glob('*.txt'))
    assert actual == profile_keys, f'Profile/query ownership mismatch: fold {fold}'
    owned.append(actual)
assert not owned[0] & owned[1]
assert owned[0] | owned[1] == {(t, q) for t, qs in sqls.items() for q in qs}
print('3200 SQLs; two disjoint folds; old folds and both profile memberships agree.')
print('PREPARATION COMPLETE. No experiment queries have been executed yet.')
PY
```

到这里，SQL、三套 error profiles、数据库统计、新代码、shared folds 都已经准备完成，并且 folds 与 profile 的 query 归属已核对。**可以先停在这里；下面才开始运行实验。** 新候选 hints、缓存、编码和模型是运行后产生的结果，不从旧实验复制。

# 第二部分：按原有命令完整运行

这一部分继续使用 0826 版的 `pg_ctl` 和正式 Python 脚本，按原顺序运行。没有新的 shell 执行函数。主要变化只有：新路径、RobDP 显式指定 `--parameter-group 8x1__0x0`，以及已更新的 Execution Time 采集代码。后续不再从 `/data/robdp` 复制输入。

下面沿用同一个 PostgreSQL 集群，版本之间按顺序 stop/start。只在这次新的结果目录中跑一遍；旧 runner 会追加输出，失败重跑时先检查部分产物，不直接叠加第二次测量。

## 4. 0A + 0D：重新运行 Original PG 和 Reqo-GUC 候选生成

0A 使用 ANALYZE 保存 baseline Execution Time。0D 只生成候选及 hints，不需要 ANALYZE；候选的实际执行统一放在步骤 2A。

```bash
/opt/pgsql/16.2.0000/bin/pg_ctl -D "${PGDATA_DIR}" start

"${REQO_PYTHON}" "${REQO_ROOT}/run_imdb_with_pg.py" \
  --dbname "${DBNAME}" --host "${HOST}" --port "${PORT}" --user "${DBUSER}" \
  --sqls-dir "${SQLS_DIR}" --workload-name "${WORKLOAD}" \
  --skip-template-id-vals 29 --query-id-limit "${QUERY_ID_LIMIT}" \
  --results-path "${RUN_ROOT}/runner_outputs/original" \
  --statement-timeout "${STMT_TIMEOUT}" --rounds 1 --run-mode explain-analyze-json \
  > "${LOG_DIR}/0A-original.log" 2>&1

"${REQO_PYTHON}" "${REQO_ROOT}/run_imdb_with_reqo_guc.py" \
  --dbname "${DBNAME}" --host "${HOST}" --port "${PORT}" --user "${DBUSER}" \
  --sqls-dir "${SQLS_DIR}" --workload-name "${WORKLOAD}" \
  --skip-template-id-vals 29 --query-id-limit "${QUERY_ID_LIMIT}" \
  --results-path "${RUN_ROOT}/runner_outputs/reqo_guc" \
  --statement-timeout "${STMT_TIMEOUT}" --rounds 1 --run-mode explain-json \
  > "${LOG_DIR}/0D-reqo-guc.log" 2>&1

/opt/pgsql/16.2.0000/bin/pg_ctl -D "${PGDATA_DIR}" stop
```

## 5. 0B：重新运行 RobDP-8 Direct

Direct 使用 global profile，沿用原对比定义。仅运行 `8x1/0x0`，不运行基础 1×。

```bash
/opt/pgsql/16.2.0524/bin/pg_ctl -D "${PGDATA_DIR}" start

"${REQO_PYTHON}" "${REQO_ROOT}/run_imdb_with_robdp.py" \
  --dbname "${DBNAME}" --host "${HOST}" --port "${PORT}" --user "${DBUSER}" \
  --sqls-dir "${SQLS_DIR}" --error-profile-root "${SQLS_DIR}" \
  --workload-name "${WORKLOAD}" --skip-template-id-vals 29 \
  --query-id-limit "${QUERY_ID_LIMIT}" \
  --results-path "${RUN_ROOT}/runner_outputs/robdp" \
  --statement-timeout "${STMT_TIMEOUT}" --rounds 1 --run-mode explain-analyze-json \
  --parameter-group 8x1__0x0 \
  > "${LOG_DIR}/0B-robdp8-direct.log" 2>&1

/opt/pgsql/16.2.0524/bin/pg_ctl -D "${PGDATA_DIR}" stop
```

## 6. 0C：两个 train folds 分别重新导出 RobDP-Gen8 候选

`train_fold=1` 用 profile 1，为后续 eval fold 2 生成候选；反之亦然。每次 `--fold-split all` 包含全部 query，使训练和测试候选都来自同一个训练
fold 的生成配置。

```bash
/opt/pgsql/16.2.0626/bin/pg_ctl -D "${PGDATA_DIR}" start

for train_fold in 1 2; do
  eval_fold=$((3 - train_fold))
  "${REQO_PYTHON}" "${REQO_ROOT}/run_imdb_with_robdp_hints.py" \
    --dbname "${DBNAME}" --host "${HOST}" --port "${PORT}" --user "${DBUSER}" \
    --sqls-dir "${SQLS_DIR}" --workload-name "${WORKLOAD}" \
    --skip-template-id-vals 29 --query-id-limit "${QUERY_ID_LIMIT}" \
    --folds-dir "${RUN_ROOT}/folds" --fold-source robdp \
    --fold-id "${eval_fold}" --fold-split all \
    --error-profile-root "${BASE}/imdb-separate-ep-0924-${train_fold}" \
    --results-path "${RUN_ROOT}/fold_${train_fold}/runner_outputs/robdp_last_level" \
    --statement-timeout "${STMT_TIMEOUT}" --rounds 1 --run-mode explain-json \
    --parameter-group 8x1__0x0 \
    --final-level-path-limit 13 \
    > "${LOG_DIR}/0C-robdp8-train-fold-${train_fold}.log" 2>&1
done

/opt/pgsql/16.2.0626/bin/pg_ctl -D "${PGDATA_DIR}" stop
```

## 7. 1A/1B：重新拼接 hinted SQL

只取正常的 `last_level_hints`，沿用 `--hint-source robdp`；不混入 partial hints。Reqo 的 13 个配置经 hint 去重后，不保证每个
query 恰好有 13 个候选。

```bash
"${REQO_PYTHON}" "${REQO_ROOT}/build_imdb_hint_sql_csv.py" \
  --results-path "${RUN_ROOT}/runner_outputs" \
  --sqls-dir "${SQLS_DIR}" --workload-name "${WORKLOAD}" \
  --output-dir "${RUN_ROOT}/hint-sql-csv" --query-id-limit "${QUERY_ID_LIMIT}" \
  --hint-source reqo --parameter-groups reqo_guc \
  > "${LOG_DIR}/1B-reqo-sql.log" 2>&1

for train_fold in 1 2; do
  "${REQO_PYTHON}" "${REQO_ROOT}/build_imdb_hint_sql_csv.py" \
    --results-path "${RUN_ROOT}/fold_${train_fold}/runner_outputs/robdp_last_level" \
    --sqls-dir "${SQLS_DIR}" --workload-name "${WORKLOAD}" \
    --output-dir "${RUN_ROOT}/fold_${train_fold}/hint-sql-csv" \
    --query-id-limit "${QUERY_ID_LIMIT}" --hint-source robdp --parameter-groups 8x1/0x0 \
    > "${LOG_DIR}/1A-robdp8-train-fold-${train_fold}.log" 2>&1
done
```

## 8. 2A/2B：重新执行候选，保存 Execution Time 缓存

现在是 **三份缓存**：共享 Reqo-GUC 一份，RobDP-Gen8 的 train fold 1/2 各一份。不是四份；Reqo-GUC 的原始缓存可供两折共用。

检查时现有数据库配置包含 `shared_preload_libraries = 'pg_hint_plan'`。切换到 hint 版本后，下面先验证新会话中扩展已加载且
hints 已启用。失败则先修正该实例配置，不继续做候选计时。

```bash
/opt/pgsql/16.2.hint/bin/pg_ctl -D "${PGDATA_DIR}" start

"${REQO_PYTHON}" - <<'PY'
import os, psycopg2
with psycopg2.connect(dbname=os.environ['DBNAME'], user=os.environ['DBUSER'],
                      host=os.environ['HOST'], port=os.environ['PORT']) as conn:
    with conn.cursor() as cur:
        cur.execute('SHOW pg_hint_plan.enable_hint')
        assert cur.fetchone()[0] == 'on', 'pg_hint_plan must be loaded and enabled'
print('pg_hint_plan is loaded and enabled')
PY

# 2A. Shared Reqo-GUC candidate cache.
cache_dir="${RUN_ROOT}/encoding/reqo_guc/full"
cache_file="${cache_dir}/plans_cache.json"
mkdir -p "${cache_dir}"

"${REQO_PYTHON}" "${REQO_ROOT}/Utils/reqo_encode_sql_save_pt.py" \
  --sql-file "${RUN_ROOT}/hint-sql-csv/reqo_guc.csv" \
  --dbname "${DBNAME}" --host "${HOST}" --port "${PORT}" --user "${DBUSER}" \
  --stats-dir "${STATS_DIR}" --output-dir "${cache_dir}" \
  --analyze --plans-cache-output "${cache_file}" --plans-cache-only \
  --statement-timeout-ms "${STMT_TIMEOUT_MS}" --min-candidates-per-query 2 \
  > "${LOG_DIR}/2A-reqo-cache.log" 2>&1

# 2B. RobDP-Gen8 candidate caches, one for each training fold.
for train_fold in 1 2; do
  cache_dir="${RUN_ROOT}/fold_${train_fold}/encoding/robdp_last_level_8x1__0x0/full"
  cache_file="${cache_dir}/plans_cache.json"
  mkdir -p "${cache_dir}"

  "${REQO_PYTHON}" "${REQO_ROOT}/Utils/reqo_encode_sql_save_pt.py" \
    --sql-file "${RUN_ROOT}/fold_${train_fold}/hint-sql-csv/8x1__0x0.csv" \
    --dbname "${DBNAME}" --host "${HOST}" --port "${PORT}" --user "${DBUSER}" \
    --stats-dir "${STATS_DIR}" --output-dir "${cache_dir}" \
    --analyze --plans-cache-output "${cache_file}" --plans-cache-only \
    --statement-timeout-ms "${STMT_TIMEOUT_MS}" --min-candidates-per-query 2 \
    > "${LOG_DIR}/2B-robdp8-train-fold-${train_fold}.log" 2>&1
done

/opt/pgsql/16.2.hint/bin/pg_ctl -D "${PGDATA_DIR}" stop
```

不设置 `--skip-errors`；普通错误立即停止，避免不知不觉丢候选。超时沿用编码器的 fallback 和惩罚策略，不算成功测量。旧 Reqo
缓存超时候选较多，因此此阶段可能耗时很长。

采集后检查三份缓存。这里逐份读取，检查标签和候选覆盖，并输出新实验实际超时数；不预设旧实验的计数。

```bash
"${REQO_PYTHON}" - <<'PY'
import os, sys, json, csv
from pathlib import Path
sys.path.insert(0, str(Path(os.environ['REQO_ROOT']) / 'Utils'))
from plan_runtime import plan_timing_metadata, execution_runtime_label
run = Path(os.environ['RUN_ROOT'])
specs = [(run / 'encoding/reqo_guc/full/plans_cache.json', run / 'hint-sql-csv/reqo_guc.csv')]
specs += [(run / f'fold_{f}/encoding/robdp_last_level_8x1__0x0/full/plans_cache.json',
           run / f'fold_{f}/hint-sql-csv/8x1__0x0.csv') for f in (1, 2)]
def key(row):
    return tuple(int(row[k]) for k in ('template_id', 'original_query_id', 'candidate_id'))
for cache, sql in specs:
    with cache.open() as file:
        rows = json.load(file)
    with sql.open() as file:
        sql_rows = list(csv.DictReader(file))
    assert len({key(r) for r in rows}) == len(rows)
    assert {key(r) for r in rows} == {key(r) for r in sql_rows}
    censored = 0
    for row in rows:
        meta = plan_timing_metadata(row)
        execution_runtime_label(meta)
        censored += meta['runtime_is_censored']
    print(cache, 'candidates=', len(rows), 'measured=', len(rows)-censored, 'timeouts=', censored)
    del rows, sql_rows
PY
```

## 9. 4A：从新 hints 生成每折 train/test SQL

```bash
for train_fold in 1 2; do
  eval_fold=$((3 - train_fold))
  "${REQO_PYTHON}" "${REQO_ROOT}/build_imdb_fold_sql_from_splits.py" \
    --folds-dir "${RUN_ROOT}/folds" --fold-source robdp --fold-id "${eval_fold}" \
    --output-root "${RUN_ROOT}" --min-candidates-per-query 2 \
    --source-csv "reqo_guc=${RUN_ROOT}/hint-sql-csv/reqo_guc.csv" \
    --source-csv "robdp_last_level_8x1__0x0=${RUN_ROOT}/fold_${train_fold}/hint-sql-csv/8x1__0x0.csv" \
    > "${LOG_DIR}/4A-eval-fold-${eval_fold}.log" 2>&1
done
```

若某 query 只有一个候选，这一步会过滤它，日志报告缺失数量；不要将它写成 timeout。0924 的有效 query 数须根据新产物统计，不能预先写死
3,191。

## 10. 4B：重新编码和转换

两来源 × 两折 × train/test，共八份 `encode.pt` 和八套 `.npy`。每个训练折重新计算归一化统计，同折 test 复用对应 train
的统计。此阶段使用缓存，不重新连接数据库。

```bash
for eval_fold in 1 2; do
  train_fold=$((3 - eval_fold))
  for source in "${SOURCES[@]}"; do
    if [ "${source}" = reqo_guc ]; then
      cache_file="${RUN_ROOT}/encoding/reqo_guc/full/plans_cache.json"
    else
      cache_file="${RUN_ROOT}/fold_${train_fold}/encoding/${source}/full/plans_cache.json"
    fi
    "${REQO_PYTHON}" "${REQO_ROOT}/encode_fold_datasets.py" \
      --source-name "${source}" --fold-id "${eval_fold}" \
      --fold-sql-root "${RUN_ROOT}/fold_sql" \
      --encoding-root "${RUN_ROOT}/encoding" --dataset-root "${DATASET_ROOT}" \
      --dbname "${DBNAME}" --host "${HOST}" --port "${PORT}" --user "${DBUSER}" \
      --stats-dir "${STATS_DIR}" --plans-cache-input "${cache_file}" \
      --statement-timeout-ms "${STMT_TIMEOUT_MS}" --min-candidates-per-query 2 \
      --repo-root "${REQO_ROOT}" \
      > "${LOG_DIR}/4B-${source}-eval-fold-${eval_fold}.log" 2>&1
  done
done
```

## 11. 5：重新训练四个模型

下面在同一路径下运行。代码会在有 CUDA 时用 GPU，否则用 CPU；全量训练建议在配置好 GPU 的机器执行。

若转到训练服务器，只需要同步：新代码、八套 datasets（包含每套 `*_conversion_summary.json`）、七个统计 `.npy`。不用传 raw
cache、encode.pt、数据库或 error profiles。目标服务器若不使用 `/data/robdp-new`
，需明确重设那台机器的路径变量；不要把数据库服务器路径原封不动用到不存在的位置。训练结果再放回这里的 `TRAIN_RESULTS_ROOT`
后执行下一节汇总。

```bash
cd "${REQO_ROOT}"
for source in "${SOURCES[@]}"; do
  for eval_fold in 1 2; do
    "${REQO_PYTHON}" "${REQO_ROOT}/train_no_split.py" \
      --dbname "${DBNAME}" --fold-id "${eval_fold}" \
      --train-dataset-dir "${DATASET_ROOT}/${source}/fold_${eval_fold}/train" \
      --test-dataset-dir "${DATASET_ROOT}/${source}/fold_${eval_fold}/test" \
      --output-dir "${TRAIN_RESULTS_ROOT}/${source}/fold_${eval_fold}" \
      --database-statistics-dir "${STATS_DIR}" --save-model \
      > "${LOG_DIR}/5-${source}-eval-fold-${eval_fold}.log" 2>&1
  done
done
```

此处保留现有训练程序和超参数；本手册没有另行改变其验证集/early-stopping 设计。训练入口会拒绝旧口径数据，不要手工给旧数据补一个
`execution_time` 标记。

## 12. 6：汇总新实验，并输出统一 query 交集的论文表

```bash
"${REQO_PYTHON}" "${REQO_ROOT}/summarize_all_groups.py" \
  --folds-dir "${RUN_ROOT}/folds" --fold-source robdp \
  --results-path "${RUN_ROOT}/runner_outputs" \
  --robdp-runtime-results-root "${RUN_ROOT}/runner_outputs/robdp" \
  --train-results-root "${TRAIN_RESULTS_ROOT}" \
  --groups 8x1__0x0 --runtime-agg mean --output-dir "${SUMMARY_DIR}" \
  > "${LOG_DIR}/6-summary.log" 2>&1
```

原汇总文件包含逐列可用数据的均值，论文对比需要统一 query 集。下面额外生成 `paper_methods_common.csv` 和
`paper_candidates_common.csv`，四个方法和两个候选集均使用同一组 query。Optimal 指该来源候选集中的最佳标签，不是全局最优计划。

```bash
"${REQO_PYTHON}" - <<'PY'
import os, csv, math
from pathlib import Path
from statistics import mean
summary = Path(os.environ['SUMMARY_DIR'])
with (summary / '8x1__0x0/fold_query_summary.csv').open() as f:
    all_rows = list(csv.DictReader(f))
fields = ['original_runtime_ms', 'robdp_runtime_ms',
          'reqo_guc_oracle_runtime_ms', 'reqo_guc_min_cost_runtime_ms', 'reqo_guc_reqo_runtime_ms',
          'robdp_last_level_oracle_runtime_ms', 'robdp_last_level_min_cost_runtime_ms',
          'robdp_last_level_reqo_runtime_ms']
def valid(row):
    try:
        return all(math.isfinite(float(row[k])) and float(row[k]) >= 0 for k in fields)
    except (ValueError, TypeError, KeyError):
        return False
common = [r for r in all_rows if valid(r)]
assert common, 'No common valid queries'
def write(name, rows):
    with (summary / name).open('w', newline='') as f:
        writer = csv.DictWriter(f, fieldnames=list(rows[0]))
        writer.writeheader()
        writer.writerows(rows)
write('paper_common_queries.csv', [
    {k:r[k] for k in ['fold_id', 'template_id', 'original_query_id'] + fields} for r in common
])
methods = [('PostgreSQL', 'original_runtime_ms'),
           ('Reqo-Gen + Reqo-Selector', 'reqo_guc_reqo_runtime_ms'),
           ('RobDP-8 Direct', 'robdp_runtime_ms'),
           ('RobDP-Gen8 + Reqo-Selector', 'robdp_last_level_reqo_runtime_ms')]
method_rows, candidate_rows = [], []
for scope in ('fold_1', 'fold_2', 'overall'):
    rows = common if scope == 'overall' else [r for r in common if r['fold_id'] == scope[-1]]
    assert rows, f'No common queries in {scope}'
    for name, field in methods:
        method_rows.append(dict(scope=scope, method=name, query_count=len(rows),
                                mean_runtime_ms=mean(float(r[field]) for r in rows)))
    for source, prefix in [('Reqo-Gen', 'reqo_guc'), ('RobDP-Gen8', 'robdp_last_level')]:
        cost = mean(float(r[f'{prefix}_min_cost_runtime_ms']) for r in rows)
        reqo = mean(float(r[f'{prefix}_reqo_runtime_ms']) for r in rows)
        candidate_rows.append(dict(scope=scope, candidate_source=source, query_count=len(rows),
            optimal_ms=mean(float(r[f'{prefix}_oracle_runtime_ms']) for r in rows),
            cost_selector_ms=cost, reqo_selector_ms=reqo,
            reduction_percent=(1-reqo/cost)*100 if cost else ''))
write('paper_methods_common.csv', method_rows)
write('paper_candidates_common.csv', candidate_rows)
print('Common queries:', len(common), '/', len(all_rows))
print('Excluded queries:', len(all_rows)-len(common))
print('Paper tables:', summary)
PY
```

`overall` 是所有共同 query 的均值；两折 query
数不同时，不用两折均值的简单平均替代它。超时惩罚若保留在候选集中，仍会进入训练或相关统计；上述交集筛选不等于排除所有超时。论文计时口径应写“成功执行使用
Execution Time，超时使用固定惩罚”，并参考步骤 2 的计数。

## 13. 最终应该得到什么

```text
/data/robdp-new/
  imdb-error-profile-0924/                  # SQL + global profile
  imdb-separate-ep-0924-1/                  # train fold 1 profile
  imdb-separate-ep-0924-2/                  # train fold 2 profile
  imdb-separate-ep-0924/
    inputs/folds-0924-reference/
    folds/                                # 两折各 1600 query
    runner_outputs/original/
    runner_outputs/reqo_guc/
    runner_outputs/robdp/8x1/0x0/
    fold_1/runner_outputs/robdp_last_level/8x1/0x0/
    fold_2/runner_outputs/robdp_last_level/8x1/0x0/
    hint-sql-csv/reqo_guc.csv
    fold_1/hint-sql-csv/8x1__0x0.csv
    fold_2/hint-sql-csv/8x1__0x0.csv
    encoding/reqo_guc/full/plans_cache.json
    fold_1/encoding/robdp_last_level_8x1__0x0/full/plans_cache.json
    fold_2/encoding/robdp_last_level_8x1__0x0/full/plans_cache.json
    fold_sql/{source}/fold_{eval_fold}/{train,test}.csv
    encoding/{source}/fold_{eval_fold}/{train,test}/encode.pt
    summary/paper_methods_common.csv
    summary/paper_candidates_common.csv
    summary/paper_common_queries.csv
    logs/
  Reqo-PG/
    run_imdb_with_robdp.py
    run_imdb_with_robdp_hints.py
    Data/database_statistics/
    Data/imdbloadbase/datasets-separate-ep-0924/
    Results/imdbloadbase/separate-ep-0924/
```

以上是目标目录结构，不是声称已经运行完成。当前只修改本地代码并编写、检查命令；服务器上的文件复制、git pull 和实验执行，都由你执行本手册时发生。
